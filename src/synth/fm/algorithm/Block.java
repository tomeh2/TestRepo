package synth.fm.algorithm;

/**
 * Basic system is any system which takes in one input, processes it and produces some output
 * @author Tomi
 *
 */
public interface Block
{
	public float process(float input);
}
